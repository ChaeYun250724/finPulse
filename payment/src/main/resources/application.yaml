spring:
  datasource:
    driver-class-name: org.mariadb.jdbc.Driver
    url: jdbc:mariadb://localhost:3306/finpulse
    name: finpulse
    password: finpulse
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    database-platform: org.hibernate.dialect.H2Dialect
    spring:
  data:
    redis:
      host: localhost
      port: 6379

server:
  port: 8080

tosspayments:
  secret-key:

payment:
  default-amount: 1000
  default-currency: KRW



